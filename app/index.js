// useAuthRouting in the root layout redirects to /sign-in / /welcome / /today
// based on session + ack status. this index renders nothing in the brief window
// before that redirect lands.
export default function Index() {
  return null;
}
